---
description: Check whether the local Kiro CLI is installed and authenticated
argument-hint: '[--json]'
allowed-tools: Bash(node:*)
---

Run:

```bash
node "${CLAUDE_PLUGIN_ROOT}/scripts/kiro-companion.mjs" setup $ARGUMENTS
```

Present the output to the user exactly as returned.
